#merging elements of a list
lista = ["Aldo", "Giovanni", "Giacomo"]
char = "&"
print(char.join(lista))

#to check if a string starts with something
string = "hello G"
print(string.startswith("hello")) #this returns true
#endswith() works in the same way

#to make the first char an Uppercase we use capitalize()
print(string.capitalize())